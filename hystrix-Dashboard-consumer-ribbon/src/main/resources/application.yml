spring:
  application:
    name: eureka-consumer

server:
  port: 5001

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka/

logging:
  file: ${spring.application.name}.log